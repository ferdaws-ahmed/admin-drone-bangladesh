/**
 * BannerUploader.jsx
 *
 * Deprecated — all upload logic is now handled inside BannerController.jsx.
 * This stub is kept so any stale imports do not break the build.
 */
export default function BannerUploader() {
  return null;
}
